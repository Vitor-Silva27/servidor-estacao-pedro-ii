{
	email {$ACME_EMAIL}
	# Sem redirecionamento HTTP: a porta 80 não é publicada no host.
	auto_https disable_redirects
}

{$DOMAIN}:8443 {
	tls {
		dns duckdns {env.DUCKDNS_TOKEN}
	}

	encode gzip
	reverse_proxy api:3333
}
